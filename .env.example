# Local Django backend on your laptop (ipconfig → IPv4 Wi‑Fi)
EXPO_PUBLIC_DEV_API_HOST=192.168.0.165

# Expo Go / web: disable Health Connect & custom native step counter (default on in Expo Go)
# EXPO_PUBLIC_DESIGN_PREVIEW=true
